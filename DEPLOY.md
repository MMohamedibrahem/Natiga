# دليل نشر الموقع على الإنترنت (Deployment Guide)

لقد تم تصميم الموقع ليعمل بـ **Node.js** وقاعدة بيانات **SQLite** محلية بمخرجات فائقة السرعة. لنشر هذا المشروع على الإنترنت مجاناً، نوصي باستعمال منصة **Render** أو **Railway**.

إليك الخطوات التفصيلية لنشر الموقع:

---

## الخطوة 1: رفع المشروع على GitHub

بدلاً من رفع ملف قاعدة البيانات الضخم `students.db` (حجمه **89.6 ميجابايت**) والذي قد يفشل GitHub في رفعه، قمنا بإنشاء ملف مضغوط باسم `students.db.gz` بحجم **25.5 ميجابايت** فقط، وهو يرفع فوراً وبسهولة.

لقد قمنا بإعداد الموقع والملفات كالتالي:
* تمت إضافة `students.db` إلى ملف `.gitignore` لمنع رفع الملف الكبير.
* تم إعداد خادم الموقع `server.js` ليفك ضغط `students.db.gz` تلقائياً عند أول تشغيل له على المنصة ليعيد إنتاج قاعدة البيانات.

افتح مبدل الأوامر (Terminal) في مجلد المشروع ونفّذ الأوامر التالية لرفع المشروع:

```bash
# 1. تهيئة مستودع Git جديد
git init

# 2. إضافة جميع الملفات (سيتم تجاهل الملف الضخم ورفع الملف المضغوط تلقائياً)
git add .

# 3. حفظ التغييرات
git commit -m "Initial commit with compressed SQLite database"

# 4. قم بإنشاء مستودع جديد على موقع GitHub (سمّه مثلاً thanawya-results)
# ثم اربط المجلد المحلي بالمستودع على GitHub واضغط لرفع الملفات:
git branch -M main
git remote add origin https://github.com/USERNAME/thanawya-results.git
git push -u origin main
```
*(استبدل `USERNAME` و `thanawya-results` برابط مستودعك الخاص).*

---

## الخطوة 2: النشر على منصة Render (الخيار الأسهل والمجاني)

تتيح لك منصة **Render** استضافة تطبيقات Node.js مجاناً وسحب الكود تلقائياً من GitHub.

1. **إنشاء حساب**: ادخل إلى موقع [Render.com](https://render.com) وسجّل دخولك باستخدام حسابك على **GitHub**.
2. **إنشاء خدمة جديدة**:
   - اضغط على زر **New +** ثم اختر **Web Service**.
   - اختر **Connect a repository** وقم بربط المستودع الذي رفعته في الخطوة 1.
3. **إعدادات الخدمة (Settings)**:
   - **Name**: اختر اسماً لمشروعك (مثلاً: `thanawya-results-portal`).
   - **Environment**: اختر **Node**.
   - **Region**: اختر أقرب منطقة لك (مثلاً `Frankfurt` أو `Singapore`).
   - **Branch**: اختر `main`.
   - **Build Command**: اتركه فارغاً أو اكتب `npm install` (المشروع لا يحتاج لمكتبات إضافية لكن يفضل تركها افتراضية).
   - **Start Command**: اكتب `npm start` (أو `node server.js`).
   - **Instance Type**: اختر الخيار المجاني **Free**.
4. **بدء النشر**:
   - اضغط على **Deploy Web Service**.
   - ستقوم المنصة ببناء وتنزيل الكود وتشغيل الخدمة. بعد انتهاء العملية (تستغرق حوالي دقيقة أو دقيقتين)، ستعطيك المنصة رابطاً عاماً مثل: `https://thanawya-results-portal.onrender.com`.

> [!NOTE]
> **ملاحظة حول الحساب المجاني في Render**:
> عندما تترك الموقع بدون زيارات لفترة، فإن الخادم يدخل في حالة "نوم" (Spin down) لتوفير الموارد. عند زيارة الموقع مجدداً، قد يستغرق حوالي 30 ثانية للاستيقاظ لأول مرة، ولكنه سيعمل بعد ذلك بسرعة فائقة.

---

## الخطوة 3: النشر على منصة Railway (بديل سريع جداً وبدون نوم الخادم)

إذا كنت تفضل سرعة تشغيل فورية وعدم دخول الخادم في حالة خمول:

1. ادخل إلى موقع [Railway.app](https://railway.app) وسجل دخولك بحساب GitHub.
2. اضغط على **New Project** واشحن المستودع من GitHub.
3. ستقوم المنصة تلقائياً بقراءة ملف `package.json` وتثبيت النسخة المطلوبة وتشغيل المشروع.
4. اذهب إلى إعدادات المشروع (Settings) واضغط على **Generate Domain** لإنشاء رابط عام لموقعك.
