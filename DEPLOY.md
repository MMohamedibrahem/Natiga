# دليل نشر الموقع على الإنترنت (Deployment Guide)

لقد تم تصميم الموقع ليعمل كـ **Serverless Application** متوافق تماماً مع **Vercel** مجاناً وبسرعة فائقة.

إليك الخطوات التفصيلية لنشر الموقع:

---

## الخطوة 1: رفع المشروع على GitHub

بدلاً من رفع ملف قاعدة البيانات الضخم `students.db` (حجمه **89.6 ميجابايت**) والذي يمنعه GitHub، قمنا بإنشاء ملف مضغوط باسم `students.db.gz` بحجم **25.5 ميجابايت** فقط.

لقد قمنا بإعداد المشروع كالتالي:
* تمت إضافة `students.db` إلى ملف `.gitignore` لمنع رفعه.
* تم إعداد خادم الموقع لفك ضغط قاعدة البيانات تلقائياً في المجلد المؤقت للـ Serverless `/tmp` عند أول طلب على الموقع.

افتح مبدل الأوامر (Terminal) في مجلد المشروع ونفّذ الأوامر التالية لرفع المشروع:

```bash
# 1. تهيئة مستودع Git جديد
git init

# 2. إضافة جميع الملفات (سيتم تجاهل الملف الضخم ورفع الملف المضغوط تلقائياً)
git add .

# 3. حفظ التغييرات
git commit -m "Initial commit with compressed SQLite database"

# 4. اربط المجلد المحلي بالمستودع على GitHub واضغط لرفع الملفات:
git branch -M main
git remote add origin https://github.com/USERNAME/thanawya-results.git
git push -u origin main
```
*(استبدل `USERNAME` و `thanawya-results` برابط مستودعك الخاص).*

---

## الخطوة 2: النشر على منصة Vercel (الخيار الموصى به والمطلوب)

تتيح لك منصة **Vercel** استضافة التطبيقات بدون خوادم (Serverless) مجاناً. لقد أضفنا ملف `vercel.json` وملف `api/student.js` لتهيئة المشروع للعمل عليها تلقائياً.

1. **تسجيل الدخول**: ادخل إلى موقع [Vercel.com](https://vercel.com) وسجّل دخولك باستخدام حسابك على **GitHub**.
2. **استيراد المشروع**:
   - اضغط على زر **Add New...** ثم اختر **Project**.
   - ابحث عن مستودع مشروعك `thanawya-results` واضغط على **Import**.
3. **إعدادات المشروع**:
   - سيتعرف Vercel تلقائياً على إعدادات المشروع.
   - تأكد من عدم تغيير أي إعدادات افتراضية (الإعدادات جاهزة في `package.json` و `vercel.json`).
4. **بدء النشر**:
   - اضغط على زر **Deploy**.
   - ستقوم المنصة ببناء ونشر الموقع في ثوانٍ معدودة.
   - بمجرد اكتمال النشر، ستحصل على رابط مباشر لموقعك (مثال: `https://natiga-drab.vercel.app`).

---

## الخطوة 3: النشر على منصة Render (كبديل خادم كامل)

تتيح لك منصة **Render** استضافة المشروع كـ Web Service:

1. ادخل إلى موقع [Render.com](https://render.com) وسجّل دخولك بحساب **GitHub**.
2. اضغط على **New +** ثم اختر **Web Service**.
3. قم بربط مستودع المشروع من القائمة.
4. اضبط الإعدادات التالية:
   - **Name**: اسم المشروع.
   - **Runtime**: `Node`.
   - **Build Command**: `npm install`.
   - **Start Command**: `npm start`.
   - **Instance Type**: اختر **Free**.
5. اضغط على **Deploy Web Service**.
